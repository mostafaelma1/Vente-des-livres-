<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Indiquez à quelle colonne correspond chaque information."
            android:textColor="@color/text_secondary"
            android:textSize="13sp" />

        <TextView style="@style/SectionTitle" android:layout_marginTop="12dp" android:text="Tableau détecté" />
        <Spinner
            android:id="@+id/spTable"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp" />

        <TextView style="@style/SectionTitle" android:layout_marginTop="12dp" android:text="Colonne société *" />
        <Spinner
            android:id="@+id/spSociete"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp" />

        <TextView style="@style/SectionTitle" android:layout_marginTop="12dp" android:text="Colonne montant *" />
        <Spinner
            android:id="@+id/spMontant"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp" />

        <TextView style="@style/SectionTitle" android:layout_marginTop="12dp" android:text="Colonne statut (optionnel)" />
        <Spinner
            android:id="@+id/spStatut"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp" />

        <TextView style="@style/SectionTitle" android:layout_marginTop="12dp" android:text="Colonne lot (optionnel)" />
        <Spinner
            android:id="@+id/spLot"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp" />
    </LinearLayout>
</ScrollView>
